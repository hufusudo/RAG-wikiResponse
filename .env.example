# 复制本文件为 .env 并填入你自己的配置（.env 不会被提交到仓库）
# 任意 OpenAI 兼容服务均可：DeepSeek / OpenAI / 智谱 / 通义 / 硅基流动…

# LLM（必填，问答功能依赖）
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=

# Embedding（可选；不填自动降级为本地哈希 Embedding，检索仍可用）
# EMBEDDING_API_KEY=
# EMBEDDING_BASE_URL=
# EMBEDDING_MODEL=

# 默认 Vault 路径（可选；也可在页面中输入）
# VAULT_PATH=
